This file contains the steps required to setup Tailscale on the machine.

Run the following command:
```bash
curl -fsSL https://tailscale.com/install.sh | sh
```

Run the following command:
```bash
sudo tailscale up
```
