This file contains the steps required to setup storage on the machine (including k8s volume mounting, etc.).

Run the following:
Create a `temp` directory on the VM host: `$HOME/coin2`.

Create a `raw` directory under the directory `temp` above.
